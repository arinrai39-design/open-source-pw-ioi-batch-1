node_modules/
dist/
build/
.next/
out/
coverage/
.turbo/

.env
.env.local
.env.*.local
!.env.example

*.log
npm-debug.log*
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

.vercel
*.tsbuildinfo
playwright-report/
test-results/

# Local MongoDB data (npm run db:local)
.local-db/

# Local file storage (STORAGE_DRIVER=local)
.local-uploads/

# Next.js rewrites this on every `next dev` / `next build`, and writes different
# contents for each ("This file should not be edited" is in its own header).
# Tracking it gave every contributor a phantom modified file.
next-env.d.ts
.env*
